package Practice;

public class PracticeSet {
    public static void main(String [] args){
        boolean a = true;
        boolean b = false;

        System.out.println("OR: "+ (a || b));
        System.out.println("AND: "+ (a && b));
    }
    
    // Give an example of logical Operator (||,&)...
    // Give an example of Bitwise Operator (~).
    // Give an example of Arithmetic Operator (+,%).  ..
    // Give an example of Ternary Operator (check eligible to vote or not).  ..
    // Give an example of Pre-inc and Post_dec  (number:8).
    // Give an example of right shiftby 3 (number =7).
    
}
